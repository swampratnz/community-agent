<!--
Fill in the three sections below. Keep them scoped to the diff itself —
do not paste secrets, tokens, env values, or hostnames here.
-->

## Summary

<!-- What changed and why, in a sentence or two. -->

## Security / privacy impact

<!-- Any effect on auth, tool gating, outbound filtering, data access scope,
     or stored data. If none, say so explicitly. -->

## How verified

<!-- Reminder: `npm run typecheck`, `npm test`, and `npm run build` must all
     be green before merge (see CLAUDE.md). Note any DB-touching or manual
     verification too. -->
