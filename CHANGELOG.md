# Changelog

All notable changes to the NZ Claude Community Agent, newest first. The format
loosely follows [Keep a Changelog](https://keepachangelog.com/). The agent's
`whats_new` tool reads this file, so keep entries user-legible and add a new
`##` dated section (or version) as part of each release.

Dates are the **Pacific/Auckland (New Zealand)** calendar day, not UTC — this
is a NZ community, and the CI that opens most PRs runs in UTC (a day behind NZ
for anything after ~noon NZST/NZDT). Get today's date with
`TZ='Pacific/Auckland' date +%F` rather than a bare `date`.

## 2026-07-19

### Added
- **`feature_flags` tool: see which optional behaviours are actually on**
  (#559): with 28 opt-in `*_ENABLED` config flags and growing, the only way
  to answer "is X actually turned on in prod?" was reading env vars on the
  deploy host directly — `community_info` only ever described *which tools
  exist*, never *which optional behaviours are enabled*. The new
  super-admin-only, read-only `feature_flags` tool renders a grouped On/Off
  listing (Moderation, Knowledge & Learning, Admin Alerts & Digest,
  Onboarding, WhatsApp, Cost/Model, Integrations) from a fixed, hand-written
  `FEATURE_FLAG_MAP` allowlist — it never enumerates the underlying config
  object, so a missing allowlist entry can only under-report a flag, never
  expose a non-boolean field like a token or URL. An anti-drift test ties
  the allowlist to every `*_ENABLED` flag in `config.ts`, so a newly-added
  flag that isn't consciously surfaced fails CI loudly. See
  docs/ARCHITECTURE.md and docs/SECURITY.md.
- **Undeliverable super-admin alerts are queued instead of dropped** (#545,
  extending #534): in a single-platform deployment, a sustained-disconnect
  alert, a background-job failure-threshold alert, or a model-triggered
  admin-notification (`report_content`/`appeal_moderation`/…) fired while that
  platform is down previously vanished — there was no connected adapter to DM
  through. All three now share one small in-memory queue and flush through the
  first adapter to reconnect. The shared queue is **priority-aware**: the
  member-reachable notification path can never evict a system disconnect/
  job-failure alert, so a member filing their daily reports during an outage
  can't crowd out the alerts admins most need. In-memory only (clears on
  restart), same as every other best-effort alert here.

## 2026-07-18

### Added
- **`unban_user` moderation action (Discord only)** (#543): closes the one gap
  #445 deliberately left open — `ban_user` was the sole punitive `moderate`
  action with no in-bot reversal path, forcing an admin who needed to undo a
  ban (mistake, changed situation, or an upheld appeal) out to Discord's own
  client UI, bypassing the audit trail, RBAC gate, CONFIRM flow, and
  conversation scoping every other action gets. `unban_user` is a sixth
  `moderate` action reusing that machinery unchanged: admin-tier only,
  CONFIRM-gated, refused for a target the bot has never seen, and audited
  identically to `ban_user`. Discord-only by design; requires the bot's
  Discord role to hold **Ban Members** (the same permission `ban_user` already
  needs), or the action fails cleanly as `Failed: …` rather than a
  false-success audit row.

## 2026-07-15

### Added
- **Weekly admin digest now shows report/suggestion backlog age, not just
  volume** (#450): the digest's open-report and pending-suggestion lines were
  bare counts, so a report filed 5 minutes ago and one that had sat untriaged
  for six weeks both read as "1 open report(s)" — a nag with no urgency
  signal. Each line now names the age of its single oldest outstanding item
  ("oldest 12d old"), from a `MIN(created_at)` aggregate
  (`oldestOpenReportAgeDays`/`oldestPendingSuggestionAgeDays`) over exactly the
  same scoped row set its existing count already reads — so the report age
  inherits `countOpenReports`'s conversation/DM scoping (an admin can never see
  the age of a report outside their scope) and the suggestion age stays
  guild-wide like its count. The same oldest-age refinement #515 added to the
  access-request line. Renders only when the paired count is already nonzero
  and the age is non-null, so a quiet week is byte-identical to before — no new
  tool, config knob, schema change, or extra model/embedding spend. See
  docs/ARCHITECTURE.md's admin-digest section.
- **Mod-alerts rate cap** (`MODERATION_ALERT_RATE_LIMIT_PER_HOUR`, #517):
  every other admin-notification path (escalation DMs, access-request
  alerts, auto-answer, `warn_user`) already had a rolling-hour cap — the
  private `mod-alerts` channel, whose entire purpose is carrying moderation
  signal, was the sole exception, so a raid/flood could bury the one alert
  admins most need during exactly that incident. A guild-wide rolling-hour
  counter now gates `Moderator.scan()`'s admin-channel notifications only
  (default 30/hour, generous enough that normal traffic never engages it);
  once exhausted, overflow collapses into a single summary line reporting
  the exact suppressed count instead of a wall of near-duplicate alerts.
  Enforcement — warning DMs, mutes, and the `member_warnings` audit trail —
  is never gated by this cap, only the notification is. See
  docs/ARCHITECTURE.md and docs/SECURITY.md's auto-moderation sections.
- **`react_to_message` on the WhatsApp Cloud API adapter** (#528): the docs'
  own recommended production WhatsApp path was the one remaining platform
  where `react_to_message` silently no-op'd — #494 extended it from
  Discord-only to Baileys WhatsApp and explicitly deferred the Cloud-adapter
  follow-up. `WhatsAppCloudAdapter.reactToMessage` closes that gap, reusing
  the adapter's existing 24h customer-service-window check, retry wrapper,
  and send-failure tracking verbatim — no new tool, RBAC tier, config knob,
  or schema change. Also fixes `ackReportedMessage`, the deterministic 👀 ack
  fired after a `report_content` submission, which previously silently
  no-op'd for any Cloud-adapter member filing a report.
- **Confirmed escalations now feed `knowledge_gaps` for curation priority**
  (#514): a member who confirms a real-time escalation (issue #479) has
  asked a human directly — the single strongest signal that documentation is
  missing — but that confirmation only ever DM'd admins and left no trace in
  the curation backlog. `recordEscalatedKnowledgeGap` now writes the question
  into `knowledge_gaps` with a new `escalated = true` column (added
  idempotently, defaulting every pre-existing row to `false`), fired
  alongside the existing admin notification and, unlike the passive
  below-floor `knowledge_search` miss, not gated by the per-user daily cap.
  The weekly admin digest surfaces the escalated subset as a nested count
  under its existing knowledge-gap line ("N of those were member-flagged —
  start here"), so curators triage the questions members cared enough to
  escalate first. Bare integers only reach the DM — no question text or
  member id — and `forget_me`/`purge_user_data` already delete these rows by
  `user_id`. See docs/ARCHITECTURE.md and docs/SECURITY.md.
### Fixed
- **`build` CI failure — `community_info` admin reply over its char cap**: the
  admin capabilities text had grown 18 chars past its intentional 2800-char cap
  (the `community_info: admin reply stays under a hard char cap` test, issue
  #367), failing the `build` job deterministically on `main`. Trimmed two
  non-load-bearing clarifiers (`" with reasons"`, `" (questions I couldn't
  answer)"`) from `ADMIN_CAPABILITIES_TEXT` — neither is referenced by the
  anti-drift coverage map, so every admin tool stays covered and the reply is
  back under the cap. Honours the test's "keep it tight, don't grow into a wall
  of text" discipline rather than bumping the cap.
- **Intermittent `security-invariants` CI crash (`tsx --test` exit 7)**: the
  `security-invariants` gate runs the suite with **no** Postgres, so every
  `runAgentTurn`-driven test emits a burst of fail-open warn/error logs
  (`ECONNREFUSED`, degraded-turn fallbacks, …). `pino` buffers those and flushes
  to stdout asynchronously; under **Node 24** each test file runs in a child
  process whose stdout is a pipe to the parent runner, and that trailing async
  flush could land *after* the parent stopped reading — surfacing as
  `write EPIPE`, an unhandled `'error'` on the stdout `Socket` that aborts the
  child (exit 7, no failing assertion, output truncated mid-line, and
  uninterceptable by any JS `uncaughtException`/`unhandledRejection` handler
  because it is a stream-error abort, not a throw). It was deterministic on
  Node 24 (reproduced 6/6 locally with a Node diagnostic report) but invisible
  on Node 22. The `test:security` script now runs with `LOG_LEVEL=silent`
  (override-able), so no test-run log output is buffered for a racy teardown
  flush — fixing it for every affected file at once rather than per-file. Adds
  `silent` as a valid `LOG_LEVEL` (a real `pino` level). The `build` job is
  unaffected (it runs against a real Postgres, so log volume stays low and no
  EPIPE occurs). Test/infra-only; no `SECURITY:` tests added or removed, so
  `tests/security-floor.json` is unchanged.
- **Auto-answer thread follow-ups no longer require an explicit @mention**
  (#523): the help-channel auto-answer summon gate (`AUTO_ANSWER_CHANNEL_IDS`,
  #477) only ever matched the *channel* a message was posted in — a member's
  own follow-up typed inside the thread the bot had just opened reported the
  thread's own id as its conversation id, never a member of the channel
  allowlist, so the very next message in the back-and-forth the feature
  exists for silently reverted to mention-required. The router now also
  treats a message as an auto-answer candidate when its conversation id is a
  live entry in the existing thread-to-parent-channel map (the same one
  CONFIRM/CANCEL and escalation replies already use), replying in place
  rather than opening a second thread. The per-channel rate cap is still
  reserved against the parent channel, not the thread, so a busy thread can't
  become an uncapped side-channel around `AUTO_ANSWER_RATE_LIMIT_PER_HOUR`;
  the TTL is creation-anchored and not refreshed by a follow-up, so a
  back-and-forth beyond the 10-minute window from thread creation still
  reverts to mention-required. No new tool, config knob, or schema change.
  See docs/ARCHITECTURE.md and docs/SECURITY.md §14.

## 2026-07-14

### Added
- **`appeal_moderation`** (#496): a member/guest could already see their own
  moderation status (`my_warnings`, #184) but had zero in-bot way to ask for
  a review — their only recourse was finding an admin's personal contact
  outside the bot entirely. `appeal_moderation` closes that gap: a
  self-scoped tool (eligibility read from `caller.platform`/`caller.userId`
  only, exactly like `my_warnings`, never a model-supplied id) that refuses
  cleanly ("no active warnings to appeal") unless the caller has at least
  one active warning, so it can't become a generic side channel to message
  admins — `suggest_improvement`/`report_content` already cover that. An
  eligible caller may attach one optional, sanitized, length-capped reason
  (same bound as `report_content`'s), which proactively DMs super admins via
  the existing `notifySuperAdmins` fan-out (`notifyAppealFiled`, reusing
  `notifyReportFiled`/`notifyReportWithdrawn`'s exact mechanism — no new
  conversation-scoped push helper). Rate-capped per caller (not per
  conversation) at one per `MODERATION_APPEAL_COOLDOWN_HOURS` (default 24h),
  in-memory/best-effort — no new table, no auto-unmute; `clear_warnings`
  remains the only way an admin lifts a mute.
- **Help-channel auto-answer mode** (`AUTO_ANSWER_CHANNEL_IDS`, Discord-only, #477): closes the gap where a member who posts a question in a help channel without mentioning the bot got nothing — the summon gate only ever fired for people who already knew the bot existed. An operator can now allowlist specific channels (e.g. a `#help` forum) where a top-level, non-addressed human post still gets an answer, contained in a new thread anchored to that post rather than posted bare into the channel. Unset/empty is the default and is byte-identical to today. Every existing control is reused unchanged — role resolution, the gated-mode guest exclusion, the tier-derived tool surface (member/guest only, never elevated), the per-user rate limit, the daily reply budget, and the repeat-question shortcut — plus two new guards: a configurable per-channel rolling-hour cap (`AUTO_ANSWER_RATE_LIMIT_PER_HOUR`, mirroring the existing `announce` rate limiter) and an explicit self/bot/webhook loop-prevention check. See docs/ARCHITECTURE.md and docs/SECURITY.md §14.
- **`MEMORY_RELEVANCE_THRESHOLD`** (#474): automatic per-turn memory recall and
  the `remember_search` tool both search past interactions via `searchMemory`,
  which previously had no relevance floor at all — every configured
  `MEMORY_TOP_K` slot was filled regardless of how relevant the match actually
  was, and because recalled messages ride in the user turn (not the system
  prompt), a low-relevance recalled message was full-price input token spend
  on every single reply, not a cache hit. `MEMORY_RELEVANCE_THRESHOLD` adds an
  optional cosine-similarity floor to that query, mirroring `knowledge_search`'s
  own relevance floor but kept separately tunable since memory recall has no
  eval fixture yet to derive a safe always-on default from. Defaults to `0`
  (no floor — byte-identical to today's behaviour); both call sites inherit
  the configured value automatically, with no new tool or RBAC surface.
- **Departed-admin proactive alert** (`DEPARTED_ADMIN_ALERT_ENABLED`, #472): closes the growth path #428 itself named and deferred. `list_admins` (#428) already let a super admin *pull* the "which current admins have left the server/group but still hold bot-admin privilege via DM" signal, but nothing ever pushed it — a departed admin's DM-tier access stayed invisible unless a super admin thought to run the tool. This adds an opt-in job, off by default, on the same 6h background-job cadence as every other proactive check: it counts departed-but-still-admin entries and DMs every super admin exactly once when that count first goes from zero to non-zero, re-arming only once every departed admin has been revoked or reinstated (a partial fix doesn't re-arm or re-nag). The DM is a bare count plus fixed pointer text — never a name, id, or platform — matching every other alert/digest signal's convention. No new tool, no schema change, no new RBAC surface; auto-revoke remains deliberately out of scope.
- **Real-time access-request alert** (`ACCESS_REQUEST_ALERT_ENABLED`, #480): a gated guest's first-ever addressed message was previously only visible to admins via a manual `list_access_requests` pull or the weekly digest's passive pending-count — this adds an opt-in (off by default) push the moment it happens. `recordAccessRequest`'s upsert now reports whether it created a fresh row (via `RETURNING (xmax = 0)`, no schema change), and the router DMs every admin — naming only the guest's platform and display name, never message content — exactly once per fresh request; a repeat ping from the same still-pending guest never notifies again. A guild-wide `ACCESS_REQUEST_ALERT_RATE_LIMIT_PER_HOUR` (default 10) bounds worst-case DM volume under a raid; requests over the cap are still recorded, just not alerted. No new tool, no new data exposure, never routed through the agent/model loop.
- **Weekly admin digest gains a week-over-week trend suffix** (`ADMIN_DIGEST_TRENDS_ENABLED`, #497): every one of the digest's ~15 bare-count signals (open reports, pending access requests, pending suggestions, stale/pending/low-rated/duplicate/conflicting knowledge, roster joined/left, muted members, max-turns failures, onboarding queue) read as a bare current-period count with no way to tell a stable plateau from a backlog that's quietly doubling. Off by default, this appends a ` (▲+N since last week)` / ` (▼-N since last week)` fragment to any line whose count has moved since the prior week's send, silent when it's unchanged or there's no prior snapshot. Backed by one new JSONB column (`admin_digest_sends.last_counts`) holding exactly the same integers the digest already sends — nothing new is collected, and a whitelist at the write boundary strips any unexpected key or non-integer value before it reaches the column. The snapshot is written every run regardless of the flag (so turning it on is useful from the very next tick) and is deliberately decoupled from the existing freshness guard: a quiet week (no DM sent) still snapshots the current counts without touching the freshness timestamp, so a silent week can neither be mistaken for a real send nor corrupt next week's delta.
- **`react_to_message` now works on WhatsApp too** (#494): closes the follow-up #231 itself named and deferred — reactions were Discord-only, so `ackReportedMessage`'s 👀 acknowledgement after a `report_content` filing silently no-op'd on WhatsApp, and every member had to get a full text reply instead of a low-noise emoji ack. `BaileysAdapter` now implements `reactToMessage`, sending a native WhatsApp reaction (for a group message, keyed to the original author's resolved JID, mirroring `delete_message`'s key construction; a group reaction whose author can't be resolved is skipped rather than sent with a guessed participant). No new tool, RBAC tier, or stored data — reuses `react_to_message`'s existing closed emoji allowlist and `isKnownMessage` target validation verbatim. `WhatsAppCloudAdapter` remains out of scope, pending its own 24h-window/template scoping pass.
- **Real-time admin escalation after a max-turns failure** (`ESCALATION_TO_ADMIN_ENABLED`, #479): closes the "member exhausts `AGENT_MAX_TURNS` and gets nothing but a static fallback, with zero follow-up" gap named by unaddressed community-feedback #478. Off by default; when on, a max-turns failure gets one appended line — "Want me to flag this for a community admin? Reply yes within 10 minutes." — and a matching pending entry, created atomically together so neither can exist without the other. A confirming "yes"/"y"/"āe" from that same caller within the window is handled entirely in the router (never the model): it's consumed single-shot, reserves a slot against a guild-wide, tier-blind `ESCALATION_RATE_LIMIT_PER_HOUR` (default 5), and DMs every `listAdmins()` admin with the member's own truncated question — the same recipient set the weekly digest already targets, just seen sooner. No new tool, no new privileged data access, no schema change.
- **Speaker-invariant system prompt for shared channels, plus cache-usage telemetry** (#508): the system prompt's `- Requester: NAME (role)` line varied on every turn from a different poster in a shared Discord channel or WhatsApp group, defeating the Agent SDK's prompt cache for the dominant multi-speaker traffic pattern (a prior fix attempt, #342, reordered the same line instead of removing it and was rejected for not addressing the root cause). The requester's display name now rides the user turn instead (a sanitized `[Requester: NAME]` tag, same sanitizer as before), so the system prompt is byte-identical across different same-role speakers in the same conversation. `execTurn` also now logs `cache_read_input_tokens`/`cache_creation_input_tokens` from the SDK result at debug level, so the actual cache-hit rate can be measured from real traffic rather than assumed. No new tool, config knob, or RBAC change; no dollar/token saving is claimed here — that's for the new telemetry to show.
- **`admin_activity`** (#492): a super-admin-only tool answering "who on the admin team is actually doing moderation/curation work" — the aggregated complement to `audit_view`'s flat, per-event log. Reports each admin's action count (split success/failure) and last-action timestamp over a configurable day window (default 30), busiest-first; never surfaces the underlying audit entries' free-text params.
- **`list_muted_members`** (#489): an admin tool closing the growth path #403 named and deferred — the weekly digest's "N member(s) currently muted" count had no in-bot path to the actual identities. Lists each muted member's id, strike count, status, and last-warning timestamp (never the reason/excerpt, which stay behind `list_member_warnings`), tagging a member whose strikes aged out of the window as `stale` with an explicit "may still be muted" hedge rather than a confirmed live mute.
- **Members are now warned before they hit the daily reply budget** (`DAILY_REPLY_BUDGET_WARN_ENABLED`, #512): previously the hard `DAILY_REPLY_LIMIT_PER_USER` cutoff was the first and only sign a limit existed. Off by default; once on, a caller whose remaining daily replies fall to `DAILY_REPLY_BUDGET_WARN_REMAINING` (default 5) or fewer gets one fixed line appended to their reply naming the remaining count — debounced to once per rolling 24h, never shown to super admins, and appended only to the live reply, never the cached copy the repeat-question shortcut replays later.

### Changed
- **WhatsApp voice-note transcription eligibility is now configurable** (`WHATSAPP_VOICE_MIN_ROLE`, #513): voice notes (#258) were hard-coded super-admin-only, silently dropping any member or guest's voice note with no reply at all — worse than any other unsupported-input path in the bot, and a real barrier for anyone more comfortable speaking than typing. An operator can now lower the eligibility floor to admin/member/guest; at the default (`super_admin`) the gate stays byte-identical to today, with no extra DB call. A new `WHATSAPP_VOICE_RATE_LIMIT_PER_HOUR` (default unlimited) caps per-sender volume once a wider population is opted in.
- **te reo Māori parity on the CONFIRM-outcome failure shell** (#493): the fixed `Failed: ` prefix on a `requireConfirm` outcome — the last deterministic send site the #266/#405 sweep still left in English — now renders as `I hapa: ` for a caller with a standing `'mi'` preference. Only the fixed shell is translated; the dynamic error text after it is untouched.

### Fixed
- **WhatsApp roster no longer wrongly marks a multi-group member "left"** (#505): in a deployment with more than one group in `WHATSAPP_ALLOWED_JIDS`, a member removed from one group had their roster row marked "left" even if they remained a participant of another — a documented gap from #407. The adapter now checks live membership across every other allowed group before writing the leave-mark, so `list_roster` and the admin digest's join/leave pulse no longer read a still-present member as departed.
- **A knowledge-candidate dedup guard could resurface a topic an admin already declined** (#509): the guard only ever did an exact string match, but the context builder's topic labels are freshly LLM-generated each run with no wording stability across runs — a declined topic could silently reappear as a new pending candidate the moment its wording drifted. It now falls back to a semantic (embedding-similarity) check when the exact match misses, reusing the same near-duplicate threshold the knowledge table itself uses, so a declined topic stays declined even under reworded phrasing.
- **Database queries can no longer hang the bot indefinitely** (#504): the Postgres pool had no statement, query, or connection timeout, so a single stuck query (a lock wait, a stalled network round-trip, disk pressure) could exhaust the connection pool and wedge every subsequent turn — including `/healthz`'s own health check, defeating the very monitoring meant to catch it. Query/connection timeouts are now on by default (15s/15s/10s), sized well above any normal query, so a hang now surfaces as an ordinary, already-handled failure instead of an indefinite wedge.

## 2026-07-13

### Added
- **`dev_team_findings` + `dev_team_verify`**: two **super-admin** tools that close the trust loop on a dev-team assessment. `dev_team_findings` lists a completed assessment's individual findings (id + claim) as a numbered pick-list; `dev_team_verify` then dispatches a fresh, skeptical agent to independently re-check ONE finding (by id or claim substring) and DMs the requester the verdict — **CONFIRMED / REFUTED / NEEDS-CONTEXT** plus a one-line rationale — when it lands (~1–2 min), reusing the durable `dev_team_watches` completion-DM poller with a new `verify` mode. Both tools carry the full dev_team_* trust floor: two-layer gating (tier-derived tool list + `assertAtLeast` re-check in the handler), audited, and every model-authored service field (finding claims, verdict, rationale) is quarantined — bracket/newline-neutralized, capped, and token-scrubbed — before it reaches chat or an unprompted DM, since an assessment is generated from the assessed repository's own content (the classic indirect-prompt-injection path). Verify is read-only against the target repo and small-cost, so like `assess` it needs no CONFIRM.
- **`list_knowledge_topics`** (#441): a **member-tier** (and guest, in open-access mode), read-only, no-argument tool that browses the *titles* of curated knowledge-base entries — the proactive "what's covered" counterpart to `knowledge_search`'s reactive search, for a member who doesn't yet know the right words to query for. Reuses `knowledge_search`'s exact scope predicate and excludes `'auto'`-provenance entries (issue #214's apparent-authority boundary); returns titles only, alphabetically, capped at `KNOWLEDGE_TOPICS_LIST_LIMIT` (default 50) with an exact "+N more" note when truncated.
- **`dev_team_backlog`** (#453): a **super-admin** tool that turns a previously-completed dev-team assessment into a tracked backlog on the dispatch service — the natural next step after `dev_team_dispatch`'s read-only `assess`, without needing a fresh `deliver` run. Carries the same trust floor as the rest of the dev_team_* surface (two-layer gating, audited, every echoed service string token-scrubbed and capped) and needs no CONFIRM, since it's a $0 transform of an already-paid-for assessment — no repo change, no model cost.
- **Background-job health is now surfaced through `/healthz`** (#476): each of the 12 opt-in background jobs (context builder, knowledge refresh, docs ingest, link check, both retention purges, embedding-model health check, admin digest, departed-admin alert, usage alert, Anthropic status check, dev-team-watch poller) already tracked its own consecutive-failure state, but the only externally visible signal was a one-time super-admin DM — invisible to an uptime monitor, and easy to miss entirely if that DM is muted. `/healthz` now includes a `jobs` field with each job's consecutive-failure count and last-run/last-success timestamps, and flips the top-level `status` to `"degraded"` once a job's tracker crosses its own alert threshold (a confirmed outage, not a single sub-threshold blip). Stays unauthenticated and byte-identical when no job has run yet; `/readyz` is untouched, so a degraded job can never roll back a deploy.

### Changed
- **Plain-language fallback notices** (#433): the standing `set_response_style` preference (issue #126) now reaches the eleven deterministic, non-model fallback and notice strings (pause notice, rate-limit notice, daily-budget notice, gated notice, permissions-changed, pending-confirmation, internal-error, max-turns, turn-failed, and both usage-limit replies) that were previously fixed English text regardless of a member's plain-language preference. A standing `'mi'` language preference still takes precedence when both are set, and the literal `CONFIRM`/`CANCEL` reply tokens stay untranslated so the parser keeps matching. Fails safe to the standard/English default on a lookup failure.
- **te reo Māori parity on the five opt-in shortcut replies** (#436): the ack, knowledge, guest-knowledge, repeat-question, and repeat-max-turns shortcuts — which skip a full agent turn and so sit outside the model-driven te reo handling — were the last named group deferred from the #266 series. A member with a standing `set_language_preference('mi')` now gets each shortcut's fixed reply in te reo; a wrapped, previously-served real answer (e.g. the repeat-question shortcut's cached reply) is left untranslated, the same "translate the shell, not the payload" rule every earlier site follows. Fails safe to English on a lookup failure.
- **General-knowledge disclaimer wording now varies instead of reading as a fixed script** (#456): member and admin feedback flagged the bot's "nothing in our knowledge base on this, so this is general knowledge…" hedge as robotic and repetitive — it was the one hedge in its four-clause family never given the "vary the phrasing, don't read as a disclaimer wall" instruction its siblings already carry, and it commonly stacked with the fast-moving-facts caveat into one long compound sentence for the same miss. The prompt now varies its tone like its sibling hedges and is told not to reflexively stack both caveats onto the same miss. Pure prompt-string edit — no new tool or change beyond phrasing.
- **Weekly admin digest now surfaces the onboarding queue** (#462): guests who joined but were never added as a member (issue #47's "joined but never added" case) had no proactive digest signal — every other admin backlog nags weekly, but this one was pull-only via `list_roster` (filter: `not_members`). The digest now adds a bare count — "N guest(s) joined but haven't been added as a member yet" — nudging `list_roster`. Suppressed on `open`-access-mode platforms, where a not-yet-added guest already has full member-tool access and the count would be a meaningless nag; off unless `ADMIN_DIGEST_ENABLED`.
- **`list_reports` gains a `targetUserId` filter** (#463): `moderation_history` and `user_history` have long let an admin narrow to one member's history before deciding whether to escalate, but `list_reports` only ever accepted `status`/`limit` — reviewing one member's report history meant eyeballing the whole feed. `list_reports` (and the underlying `listReports()` repository call) now accepts an optional `targetUserId`, mirroring the filter #83 already gave `moderation_history`. The new filter is applied as an additional `AND` after the existing conversation/DM/accused-admin scoping, so it can only narrow an already-scoped result set — a scoped admin still can't use it to surface a DM report filed against themselves (issue #197's exclusion, re-verified by an extended security test).

### Fixed
- **`startDevTeamWatchPoller` now alerts on consecutive failures** (#452): the dev-team completion-DM poller was the last background job with no consecutive-failure escalation to super admins — it swallowed every error into a log line only, so a dead endpoint, expired `DEV_TEAM_AUTH_TOKEN`, or DB outage left an operator's dev-team completion DM silently and indefinitely undelivered, indistinguishable from "still running." It now steps the same shared job-failure tracker every other background job uses, at a cadence-scaled threshold (`statusCheckAlertThreshold`, the same helper `anthropic-status-check` uses) rather than the flat 3-failure threshold — appropriate since its own poll interval defaults to 1 minute, far faster than the other jobs' fixed 6h tick. A subsequent success resets it silently. No behaviour change unless `DEV_TEAM_ENABLED`; `runDevTeamWatchOnce`'s own per-watch best-effort retry is unchanged.
- **WhatsApp no longer leaves members staring at "Waiting for this message"** (#459): the Baileys adapter never serviced WhatsApp/Signal **retry receipts** — the resend request a recipient's client sends when it can't decrypt a message. Baileys can re-encrypt and resend the original, but only if the socket can hand back the plaintext through a `getMessage(key)` callback, and the adapter wired up neither that callback nor any record of what it had sent. So every retry receipt went unanswered and the recipient was left on the placeholder indefinitely — hitting proactive/outbound messages (super-admin alerts, DMs, group welcomes) hardest, since those are what a fresh or desynced Signal session most often fails to decrypt on first delivery, while inbound replies decrypted on our side were unaffected. The adapter now keeps a bounded in-memory cache of recently sent messages (1000 entries / 6h) and resolves retry receipts from it, so a recent message that failed first delivery is transparently resent instead of hanging. Best-effort by design: a message aged or evicted out of the cache simply behaves as before (no auto-resend), so there is no regression, only recovery for the common recent-message case that dominates real retry receipts.
- **Member-facing knowledge citations now surface a dead-link caveat instead of a stale "last verified" claim** (#465): the weekly opt-in link-rot checker (#448) already flagged an entry's `source_unreachable`/`source_checked_at` and surfaced it to admins via `list_knowledge`, but `searchKnowledge`/`searchKnowledgeLexical` — the query behind `knowledge_search` and both knowledge shortcuts — never selected those columns, so a member citing an entry whose link had since 404'd was still told `last verified ~2 months ago`, reading as freshly-confirmed when the bot's own job already knew otherwise. `formatKnowledgeCitationNote` now renders `⚠️ link appears dead (checked …)` in place of the `last verified` clause strictly when `source_unreachable === true`; a `null` (never-checked) value renders byte-identical to before, so the caveat can never fire as a false, unearned warning on an entry the opt-in job hasn't reached yet. No schema change, no new tool, no new config knob.
- **WhatsApp Cloud sends now survive a `429` rate limit instead of dropping the reply** (#470): `sendChunk`/`uploadMedia`/`sendImageMessage` treated a `429` from Meta's Graph API — a normal response under a per-number messaging-rate limit, not an outage — identically to a permanent failure like a revoked token: the member's reply was dropped, and a legitimate traffic burst (an announcement fan-out, several members messaging at once) could produce enough genuinely-consecutive 429s to falsely trip the `isConnected()`/`/healthz` disconnect alert. All three real-send paths now retry exactly once on a `429`, honoring Meta's `Retry-After` header (clamped to 5s) and reusing the already-built, already-redacted request body unchanged — so a transient rate-limit self-heals silently, while a sustained failure (or a real outage) still surfaces exactly as it does today, one retry-delay later.

## 2026-07-12

### Added
- **`ban_user` moderation action (Discord only)** (#445): a durable exclusion rung above `kick_user` on the escalation ladder — kicking only removes someone "for now" (any live invite lets them rejoin), so a repeat offender an admin has already warned, timed out, and kicked previously forced the admin out to Discord's own client UI, bypassing the audit trail, RBAC gate, CONFIRM flow, and conversation scoping. `ban_user` is a fifth `moderate` action inheriting all of that machinery unchanged: admin-tier only, CONFIRM-gated, refused for a target the bot has never seen, and audited identically to `kick_user`. Discord-only by design (neither WhatsApp adapter has a durable "ban a phone number" primitive); requires the bot's Discord role to hold **Ban Members**, or the action fails cleanly as `Failed: …` rather than a false-success audit row. No `unban_user` counterpart — unban via Discord's own UI.
- **WhatsApp Baileys groups now feed the server roster** (#408): join/leave tracking and the roster-growth digest signal (#47, previously Discord-only — `rosterCounts('whatsapp')` was permanently zero) now cover WhatsApp groups run through Baileys. Participant add/remove events upsert/leave-mark roster rows independent of `WHATSAPP_WELCOME_ENABLED`, and a once-per-connection backfill seeds every current participant of each in-scope group. Scoped by the existing `WHATSAPP_ALLOWED_JIDS` gate and excludes the bot itself; identity metadata only (no message content, no display name — Baileys' participant events carry none). `list_roster('whatsapp', …)` and the roster-growth digest line now "just work" with no schema, tool, or config change. Documented limitation: a single `(platform, user_id)` row can't express per-group presence, so a `remove` from one allowed group marks the person "left" even if they remain in another.
- **`dev_team_dispatch` / `dev_team_status` / `dev_team_result`** (#421): three **super-admin** tools that let Dave drive a remote dev-team build service over the tailnet — dispatch a job (a read-only `assess`, or a CONFIRM-gated `deliver` that makes changes/opens a PR), check a job's status, and fetch its result — plus a proactive DM to the requester when a run finishes (typically ~20 minutes), so a super admin doesn't have to keep polling. Text-only so it works on both Discord and WhatsApp. `deliver` never dispatches until the super admin's own out-of-band CONFIRM reply, the same gate as `redeploy_bot`/`purge_user_data`. The service endpoint is reached only over an encrypted, device-authenticated Tailscale tailnet; its auth token is sent as a bearer header and redacted from logs and outbound text like every other secret.
- **`list_member_warnings`** (#414): an **admin** tool that reads back one member's full auto+admin warning history — `moderation_history` only ever showed admin-issued actions, so the majority of real warnings (auto-detected by the wordlist/LLM classifier and written straight to `member_warnings`) were invisible to an admin deciding whether to escalate, despite `my_warnings` promising members that detail was there. Shows each warning's source, reason, excerpt, and who cleared it if cleared; scoped to `(platform, userId)`, same as `clear_warnings`.
- **`engagement_stats`** (#423): a **super-admin**, read-only tool answering "what fraction of the server's currently-present membership has ever posted?" — a gap neither `usage_stats` (volume over a date range) nor `list_roster` (join/leave counts) covered. Returns an overall and per-platform engaged/total/percentage breakdown only — no per-member identity or "who hasn't posted" list — gated to super-admin since it's a guild-wide, cross-conversation aggregate.

### Changed
- **te reo Māori parity on the router's CONFIRM/CANCEL flow** (#406): the CONFIRM/CANCEL intercept and its "⚠️ Pending: …", cancelled, and permissions-changed notices now render in te reo for a member with a standing `set_language_preference('mi')` — the ninth and last deterministic send site in the #266 series, and not admin-only (`forget_me` confirms at the guest floor, so a guest deleting their own data previously hit an English-only wall at the confirmation moment). Fixed human-authored strings; the literal `CONFIRM`/`CANCEL` reply tokens stay untranslated so they keep matching. Per-tool outcome strings stay English by design.
- **`knowledge_search` now warns the model when two hits may disagree** (#393): #330's conflict detection was admin-offline-only, leaving a window between an entry being saved and an admin reconciling it where a member could hit the exact conflicting query with no signal. When two or more returned hits clear the relevance floor and a pair falls in the same mid-band similarity window `list_knowledge_conflicts` uses, the results gain one fixed trailing caveat line (never naming which entries or their content) and a `GUIDELINES` clause tells the model to say the community notes aren't fully consistent and suggest confirming with an admin, rather than silently picking one or blending them. The deterministic knowledge shortcuts are structurally single-hit and untouched.
- **Admin+ `WebSearch` is now per-conversation rate-capped** (#415): the bot's one metered, real-cost built-in tool — granted to admin and super_admin turns — previously had no cap of its own beyond the overall per-turn step limit, unlike the other admin cost levers (`warn_user`/`announce`/`create_poll`/`create_thread`, closed by #315). A new rolling-hour cap (`AGENT_WEB_SEARCH_RATE_LIMIT_PER_HOUR`, default 20) now applies per conversation, and fails closed — an internal error in the check denies the search rather than silently allowing it.

### Fixed
- **Knowledge-grounded answers from the `knowledge_search` tool are now attributable to the entry that backed them** (#413): `meta.knowledgeEntryId` was only ever stamped by the deterministic knowledge-shortcut path, so an answer the model built by calling the `knowledge_search` *tool* — the common case — carried no link back to the entry. That made the majority of knowledge answers structurally invisible to `list_low_rated_knowledge` and `list_answer_feedback`'s grouped view, no matter how often members rated them unhelpful. The turn now threads the top qualifying hit's id through to the outbound record — success turns only, never a failed or max-turns reply, which must not carry a stale correlation. Best-effort by design: a turn that searches twice but answers from the first hit stamps the second.
- **`startUsageAlert` now alerts on its own check failures** (#427): the hourly usage-alert job swallowed every `usageStats(1)` failure into a log line only — the last background job with no consecutive-failure escalation to super admins (the pattern #322 established for the status poller). It now steps its own independent failure tracker alongside the existing usage-threshold latch, DMing super admins after 3 consecutive failures via the shared job-failure-alert template; a subsequent success resets it silently. The two alert paths (threshold-crossed vs. check-failed) never suppress or duplicate each other.

## 2026-07-11

### Added
- **`AGENT_MODEL` is now optionally tiered by role** (#382): an operator can set `AGENT_MODEL_MEMBER` to run a lighter/cheaper model for member/guest turns — the highest-volume, lowest-trust tier per #347's own framing — while admin/super_admin keep `AGENT_MODEL` unchanged. Unset (the default) is byte-identical: every role resolves to `AGENT_MODEL` exactly as before. A direct lever for the shared weekly Max usage pool named in `docs/SECURITY.md`'s subscription-auth caveat; extends `buildQueryOptions`'s existing `atLeast(role, 'admin')` role split (the same one #347 uses for `maxTurns`) to the one field it didn't yet cover.
- **`list_events`** (#390): a **member-tier**, read-only, no-argument tool that answers "what's coming up?" from Discord's own Scheduled Events — the read counterpart to the admin-tier `create_event` (#242), which members had no way to read back. Returns name/start/end/location/description (deliberately no creator or organizer id), filtered to scheduled/active events, sorted soonest-first, capped at a fixed limit and ~60s-cached guild-wide so a channel asking "what's on?" doesn't fetch per turn. Surfaced in `community_info`; WhatsApp cleanly reports it's unsupported.
- **`AGENT_MODEL_CLASSIFIER` extends role-model tiering to the background classifiers** (#395): a sibling of `AGENT_MODEL_MEMBER` (#382) for the two tool-less, single-turn, fixed-format background `query()` calls #382 didn't cover — the opt-in LLM abuse classifier (`classifyAbuseWithLlm`) and the weekly context-builder cluster summariser (`summarizeCluster`) — so an operator can run a lighter/cheaper model there. The multi-turn, WebSearch-using `researchTopic` knowledge-refresh call is deliberately excluded. Unset (the default) resolves both to `AGENT_MODEL` exactly as before, byte-identical.

### Changed
- The weekly **admin digest**'s currently-muted-member count (#357) gained a hedged sub-signal for a cohort it deliberately excludes (#403): a member who was actually muted, then went quiet long enough that all their strikes aged out of the opt-in `MODERATION_STRIKE_WINDOW_DAYS` window, drops out of that count with no other admin-facing signal left that they're still blocked (nothing auto-unmutes; `clear_warnings` is still the only way out). `countStaleMutedMembers` surfaces this as "N more may still be muted from an earlier strike that's since aged out — check `moderation_history`" — hedged, not asserted, since the count is a provable upper bound rather than an exact "still muted" signal. Inert (no query) unless the window is configured; carries only a bare integer.
- **`usage_stats` and the usage alert now account for background-job cost** (#402): the moderation LLM classifier, context-builder summariser, and knowledge-refresh researcher each spawn a `query()` against the shared Max pool but wrote no `interactions` row, so their spend never reached `usageStats()` — the sole backing for the super-admin `usage_stats` tool and the `USAGE_ALERT_DAILY_REPLIES` alert. A new `background_job_costs` table (fixed job enum + numeric cost only — no ids, content, or free text) records each call's cost fire-and-forget, and `usage_stats`/the alert DM now surface it as a **separate** figure, never summed into the existing conversational-reply cost so that number keeps its meaning. Inert unless one of the three opt-in features is enabled; a failed cost write can never block or fail the underlying job.
- The weekly **admin digest** gained two knowledge-hygiene pair signals (#379): a **near-duplicate** count (`🔀`, nudges `list_duplicate_knowledge` (#316)) and a **conflict-candidate** count (`⚖️`, pairs that may disagree, nudges `list_knowledge_conflicts` (#330)) — the last two pull-only knowledge audit tools with no proactive push. Guild-wide bare-integer counts, each rendered only when `> 0`, off unless `ADMIN_DIGEST_ENABLED`.
- The weekly **admin digest** now surfaces **member turns that hit the max-turns wall** (#373): a bare `⏱️` count of replies in your conversations that exhausted `AGENT_MAX_TURNS`/`AGENT_MAX_TURNS_MEMBER` (or replayed the #310 repeat-max-turns shortcut) this week — the one member-facing failure mode the digest didn't cover. Already computed in `core.ts`, now stamped onto the outbound `interactions` row (`meta.maxTurnsExceeded`) and counted conversation-scoped. Count only, off unless `ADMIN_DIGEST_ENABLED`.
- The **pending knowledge-candidate** digest line (#285) gained an optional staleness sub-signal (#399): with `KNOWLEDGE_CANDIDATE_STALE_DAYS` set (opt-in, `0`/unset = off, floor 14 — deliberately below `KNOWLEDGE_STALE_DAYS`' 30-day floor, since a review queue should turn over sooner), the line extends to "N pending, M unreviewed for Xd+" so an old candidate can't silently sink off the default page. `list_knowledge_candidates` also gained an `oldestFirst` argument for the same view on demand. No auto-expiry — accept/decline stay the only status changes.
- **`KNOWLEDGE_STALE_MAX_AGE_DAYS`: an absolute content-age ceiling for knowledge staleness** (#381): the shared staleness predicate (behind the digest stale count, the "may be outdated" citation caveat, and the near-tie freshness tie-break) treated an entry as fresh if it was edited **or retrieved** recently — so a popular entry's clock reset on every serve, making the caveat structurally blind to the highest-reach entries. This opt-in knob (default `0` = off, floor 90 days, and never shorter than a nonzero `KNOWLEDGE_STALE_DAYS`) fires on `updated_at` alone, OR-ed into the same predicate everywhere. Unset is byte-identical to today.
- **te reo Māori on `runAgentTurn`'s failure fallbacks** (#397): the four fixed non-model failure replies (internal error, max-turns, generic turn-failed, and the upstream usage-limit reply in both its admin-notified and plain forms) now return their te reo variant for a caller with a standing `'mi'` preference — extending the #266 series to the failure path. Gated purely on the outcome being a failure, never on matching the text, so a genuine model answer is never rewritten; a `getLanguagePreference` failure still degrades to the English default.

### Fixed
- **Permanent embedding-model wedge fixed; embedding failures now logged and alerted** (#377): a single failed model load (corrupted cache, disk-full, OOM, a first-download blip) left a *rejected* promise cached in `getExtractor()` — still truthy, so every later `embed()` replayed that same rejection for the life of the process, killing knowledge search and memory recall until a full restart. The cache is now cleared on failure so the next call retries from scratch. Separately, `searchKnowledge`'s previously-silent embed-failure path now logs a warning, and a new unconditional `embedding-model` health-check job (a fixed local probe on the existing 6h/3-failure tracker, no enable flag — it's a zero-cost self-test) DMs super admins if embedding stays broken. Alert body is the usual job-name/count/timestamp only.
- **admin-digest job failures now alert** (#387): `startAdminDigest` was the last opt-in background job not wired into the consecutive-failure tracker (#265/#291) — and couldn't be, since it swallowed every error and never threw. `runAdminDigestOnce` now lets a `listAdmins()` failure propagate and throws only when every attempted admin's send failed (a freshness skip or quiet-week no-send still counts as success), and routes through the shared `startTrackedJob` path, so a persistently-failing digest pages an operator instead of going quietly unmet. No behaviour change unless `ADMIN_DIGEST_ENABLED`. (Moving to the tracker's 6h tick is idempotent — each admin's ~weekly freshness guard is unchanged.)
- **WhatsApp membership-scope staleness closed for the bare-LID removal case** (#375): the last open case in the #289/#329/#352 WhatsApp series — a Baileys `group-participants.update` `remove` carrying only a participant's privacy `@lid` JID couldn't invalidate that person's phone-number-keyed membership-cache entry, leaving a just-removed admin's tool scope live for up to the ~60s TTL. The adapter now opportunistically remembers the LID→phone pairing it already resolves for every group message (and previously discarded) and uses it to also evict the phone-keyed entry on removal. Strictly narrows scope — the mapping is only ever read to *delete* a cache entry, never to grant one — and self-prunes on use. Residual gap (documented): a participant the bot never saw post has no learned mapping.

## 2026-07-10

### Added
- `list_knowledge_conflicts` (#330): an **admin**, read-only audit tool that surfaces same-scope knowledge entry pairs about the same topic worded differently enough that they might disagree — a mid-range embedding similarity band sitting between `knowledge_search`'s relevance floor and `list_duplicate_knowledge`'s near-duplicate threshold. Sibling of `list_duplicate_knowledge` (#316), same shape and gating, opposite band; each pair is framed as a candidate for admin review (merge via `update_knowledge` or retire via `delete_knowledge`), never a confirmed contradiction. No new table, no member-facing change, no CONFIRM.
- **Repeat-max-turns shortcut** (opt-in, `REPEAT_MAX_TURNS_SHORTCUT_ENABLED`, off by default) (#310): the sibling of the repeat-question shortcut for the one outcome that one excludes — a turn that exhausted `AGENT_MAX_TURNS` without answering. When the same caller resends the exact text of a message that just failed with "too many steps" within the 2-minute window, the bot replays the fixed "try breaking it into smaller questions" reply instead of spending a second full, guaranteed-to-repeat max-turns run (the single most expensive turn class).

### Changed
- The weekly **admin digest** gained three more proactively-pushed signals, all conversation-scoped and off unless `ADMIN_DIGEST_ENABLED`: a **low-rated-knowledge** count (#326, entries members keep rating unhelpful — nudges `list_low_rated_knowledge`), a **roster growth pulse** (#346, members joined/left this week — nudges `list_roster`), and a **currently-muted-member** count (#359, distinct members at/over the strike limit — the last standing admin queue with no digest line). Each renders only when non-zero and carries only counts, never names or content.
- **`rate_answer` now accepts an optional short free-text reason** (#355) alongside the helpful/unhelpful cue (the deferred follow-up to #118/#354). When a member gives a reason in the same message as the rating, it's stored (max 200 chars, sanitised) so admins reviewing feedback see *why*, not just the score — the bot never invents a comment or asks a follow-up question just to solicit one.
- Answers built substantively on an **unreviewed auto-researched** knowledge hit are now phrased with an explicit "hasn't been reviewed by an admin yet" caveat instead of trusted-FAQ confidence (#320) — closing the gap where a member could only tell an `'auto'`-provenance entry from a curated one if the model happened to volunteer it. Complements the citation posture from #214 (which withholds source lines from quarantined `'auto'` entries). Pure `GUIDELINES` prompt edit.
- Members served via the **knowledge shortcut** now get a low-rated-answer caveat (#343): when the served entry's global unhelpful count is at/above `KNOWLEDGE_LOW_RATED_CAVEAT_MIN_UNHELPFUL` (opt-in, `0`/unset = disabled, must be `>= 2` so one rater can't trigger it), the deterministic citation note appends "other members found this unhelpful — you can flag it too with `rate_answer`".
- **`AGENT_MAX_TURNS` is now tiered by role** (#349): member/guest turns get a lower agentic-loop ceiling (`AGENT_MAX_TURNS_MEMBER`, default 6) than admin+ turns, matching the far smaller, lower-risk member tool surface (no WebSearch) — tightening the named `docs/SECURITY.md` §3 cost-runaway control for the highest-volume, lowest-trust tier without touching admin depth.
- **te reo Māori parity across the last deterministic send paths** (#334, #336, #341): a member with a standing `set_language_preference('mi')` now gets the auto-moderation warn/block DMs (#334), the membership-approval / admin-grant / suggestion- and report-resolution confirmation DMs (#336), and the code-answers policy note appended to replies (#341) in te reo Māori, using fixed human-authored translations (macrons preserved) — extending the #266/#283/#303 series so no member-facing deterministic message is left in English for an `mi`-preference member.
- **te reo Māori on the gated-guest notice** (#365): `GATED_NOTICE` — the "member-only, ask a community admin" reply sent to every gated guest — was the last of the router's four deterministic notices not to honour a standing language preference (the other three got it in #303). A guest who set `set_language_preference('mi')` while a member and was later removed (which never touches `language_prefs`) now gets the te reo variant. Fixed human-authored string; the lookup fires only on the branch that actually sends the notice, and degrades to English on a lookup failure.

### Changed
- **The gated-mode "member-only" notice now names an actual admin** (#360): a guest who addresses the bot before being added was told to "ask a community admin" without ever being told who. The notice now names up to 3 known admins by their resolved display name (e.g. "Ask a community admin — Alice or Bob — ..."), TTL-cached so this doesn't add a DB read per message. Falls back to today's exact wording, unchanged, whenever no admin display name is known. Reuses existing `community_users`/`server_roster` data (no new table); never exposes ids, emails, or phone numbers. Each name is run through `sanitizeName` (the same treatment every other display name gets before reaching outbound text, issue #227) before this auto-sent, unsolicited notice goes out, so an admin nickname can't smuggle Markdown links or embedded newlines into it.

### Fixed
- **`generate_image` now works on the WhatsApp Cloud API adapter** (#356) — the docs' own recommended production WhatsApp path. `WhatsAppCloudAdapter` was the only adapter missing `sendImage`, so the tool silently replied "not available on whatsapp" there while working fine on Discord and Baileys WhatsApp. Implemented as two Graph API calls (media upload, then a message referencing the returned media id) over the same authenticated connection `sendChunk` already uses — no new egress destination, config, or RBAC surface. Same 24h customer-service window check and caption secret-redaction as every other Cloud send.
- The **Anthropic status-check poller** now raises a consecutive-failure super-admin alert (#322): `pollAnthropicStatus` degrades silently to its last-known-good cache, so `check_status` kept answering with an honest-looking "(checked N ago)" age even if the poller had been dead for days (DNS change, egress block, Statuspage format change). It now uses the same background-job failure tracker the other jobs use, so a persistently-dead poller pages an operator instead of quietly serving stale status.
- The **upstream usage-limit alert now fans out to every connected platform** (#327): a shared Claude Max-pool exhaustion is by definition cross-platform, but the alert only reached super admins on the platform that saw the failing turn — it now loops every connected adapter, matching every other super-admin alert path.
- **Total-failure detection for the opt-in background jobs** (#340): docs-ingest, knowledge-refresh, and context-builder deliberately swallow *per-item* failures so one bad page/topic/cluster doesn't abort a run — which meant their run functions always resolved normally even when *every* item failed, so the consecutive-failure alert (#265/#291) could never fire for a wholesale outage (e.g. the `llms.txt` index reshaping). A run where the whole batch fails now signals failure into that same tracker.
- **Membership-scope staleness closed for the remaining Discord cases** (#329, #352): the ~60s admin conversation-scope cache is now invalidated on a channel permission-overwrite edit (#329) and on role removal / role-permission edits / role deletion (#352) — the last two residual cases `docs/SECURITY.md`'s "Membership-scope staleness" note named (following #286/#289), so a revoked admin's data scope no longer lags up to the full cache TTL.
- **`moderate`'s `delete_message` now validates the target id before CONFIRM** (#314): a missing `messageId` is refused immediately instead of sailing through the CONFIRM/audit gate (burning the admin's one out-of-band CONFIRM reply and writing a failed-but-recorded audit row) only to fail deep in the Discord adapter; the CONFIRM prompt also now names which message will be deleted.
- **Default welcome text is now access-mode aware** (#353): on an `open`-access deployment the hardcoded default welcome wrongly told a new joiner the bot is gated and needs admin approval — backwards, since open mode lets a guest use member tools immediately. Each join/first-contact site now picks a mode-appropriate default; the admin-configured `set_welcome_message` override path is untouched.

## 2026-07-09

### Changed
- **`warn_user` and `announce` are now per-conversation rate-capped** (#315), closing the last gap `docs/SECURITY.md`'s residual-risks section named: these were the only two of the four non-CONFIRM admin actions (`warn_user`/`announce`/`create_poll`/`create_thread`) with no throttle at all, despite `announce` being the higher-consequence sibling of `create_poll`. Mirrors the exact rate-cap-not-CONFIRM pattern `create_poll`/`create_thread` already use (issues #228/#229) — a new hardcoded per-conversation rolling-hour cap for each (`WARN_USER_RATE_LIMIT_PER_HOUR`=10, `ANNOUNCE_RATE_LIMIT_PER_HOUR`=5), in-memory, no new tool or data access. Bounds how many fabricated DM "warnings" or repeated announcements a hijacked/injected admin turn could force per conversation per hour.
- The system prompt's general-knowledge carve-out (#298) no longer tells the model to answer *every* Claude/API question with no knowledge-base hit "confidently, same as always". Durable/conceptual questions (temperature vs `top_p`, how to structure a system prompt, etc.) are unchanged. But fast-moving Anthropic-specific facts with no KB hit — current model names/versions, pricing, rate limits, feature/endpoint availability — now get a brief, natural caveat that the answer may have changed since training, plus a nudge to check current Anthropic docs or ask an admin. Complements the existing KB-hit recency hedge (#27) and docs-ingest/knowledge-refresh features rather than duplicating them: it only fires on a miss, never double-hedging a hit. Pure prompt-string edit — no new tool, tier, input surface, or stored data.
- The weekly **admin digest** now includes a **pending knowledge-candidate** count (#285): the `list_knowledge_candidates` review queue (#102) was the one such queue with no proactive signal, so a backlog sat unseen unless an admin remembered to poll it. Conversation-agnostic count only, off unless `ADMIN_DIGEST_ENABLED`.
- **te reo Māori preference honoured on more surfaces** (#283, #303): a member with a standing `set_language_preference('mi')` who leaves and rejoins the Discord server now gets the te reo welcome DM (#283), and the router's three deterministic non-agent notices — bot-paused, per-user rate-limit, and daily-reply-budget — are now sent in te reo for an `mi`-preference member too (#303). Fixed human-authored translations, extending the #266 series.
- `knowledge_search` now **breaks a near-tie in favour of the fresher entry** (#309): within a narrow relevance margin (`KNOWLEDGE_TIE_MARGIN`, 0.03), a non-stale hit sorts ahead of an equally-relevant stale one — closing the ranking half of #214 (which only ever added a passive "(may be outdated)" display hedge, never changed hit order). A genuine relevance gap still always wins.
- `report_content` now **flags repeated same-target DM reports to super admins** (#307): a member who knows an admin's platform id could repeatedly name that admin as the target of unrelated DM reports and quietly starve them of visibility via the accused-admin exclusion; a super admin is now alerted when one reporter names the same target across several DM reports in a rolling window. Read-only count, narrows a `docs/SECURITY.md` residual risk from #197.

### Fixed
- **Image generation works again** (`generate_image`). Grok removed the `GenerateImage` tool the bot was invoking, so every generation had been failing with an agent-build error; image generation is now grok's `/imagine` skill. Reworked the integration to call it and re-established the security lockdown without the old `--tools` allowlist (no longer possible). Containment is a custom **bubblewrap-enforced deny sandbox** (`--sandbox imagegen`, written by the bot) that kernel-blocks reads of the bot's secrets (`.env`, WhatsApp auth) and blocks child-process network — grok *refuses to start* if bubblewrap is missing, so it fails closed — plus **no `--always-approve`**, `--disable-web-search`, and a per-process self-check that disables image gen if the kernel deny ever stops enforcing. Verified end-to-end on the host: a real image is produced, while a prompt trying to read `/opt/community-agent/.env` is kernel-denied with nothing leaked. **Requires `bubblewrap` on the host**, and the systemd unit must allow bubblewrap's namespaces (`RestrictNamespaces=user mnt pid net`, now set in `deploy/community-agent.service`) — otherwise image gen fails closed with a "permission denied … sandbox probe" error (see docs/DEPLOYMENT.md). No behaviour change unless `IMAGE_GEN_ENABLED=true`. See docs/SECURITY.md §8.
- **Membership-scope staleness narrowed** (#289): a Discord guild/channel removal or a WhatsApp group removal now immediately invalidates that user's ~60s cached conversation scope, instead of letting a removed admin retain data scope for up to the cache TTL — closing part of a `docs/SECURITY.md`-documented residual risk (extended further in #329/#352).

## 2026-07-08

### Added
- **Multi-choice polls + `end_poll`**: `create_poll` gained a `multiChoice` option so an admin can post a poll where voters pick more than one option (defaults to single choice, so nothing changes unless asked). A new admin `end_poll` tool closes a running poll early — it freezes the tally and stops further voting without deleting the poll or its votes. Both are Discord-only, conversation-scoped, per-conversation rate-capped, and audited like `create_poll`. Note (a Discord limit, not ours): **polls can't be edited or converted after posting** — the question, options, duration, and single-vs-multi setting are fixed at creation, so switching an existing poll to multi-choice means ending it and posting a new one, which starts with zero votes.
- Weekly **admin digest** now includes a **knowledge-gaps** count (#246): the number of questions in your conversations that hit the knowledge base with no good answer this week, with a nudge to run `list_knowledge_gaps` — the one admin signal that was still pull-only is now pushed proactively like the rest. Conversation-scoped and off unless `ADMIN_DIGEST_ENABLED`; the DM carries only the count, never the questions or who asked.
- `suggest_issue` (opt-in, `GITHUB_ISSUE_ENABLED`, off by default): a **super-admin** tool to file a GitHub issue on the repo straight from a Discord or WhatsApp message — an idea, bug, or feature request becomes tracked work without leaving chat. Requires confirmation (it creates a public artifact), is rate-capped per day, scrubs secrets out of the body before sending, and is audited like every privileged action. Filed issues default to the `community-feedback` label, so they feed the research pipeline as evidence rather than skipping review. It's the bot's only outward write capability, so it holds a **fine-grained token scoped to `Issues: write` on one repo** and nothing more (see docs/SECURITY.md + docs/DEPLOYMENT.md).
- **WhatsApp voice notes** (opt-in, `WHATSAPP_VOICE_ENABLED`, off by default, Baileys only): a **super admin** can now send the bot a voice message and have it actioned. The note is transcribed locally (transformers.js Whisper — no external API, no extra key, audio never leaves the host; requires `ffmpeg`) and then runs through the exact same pipeline as a typed message, so every tool, tier check, and CONFIRM still applies. The super-admin gate runs *before* any media is downloaded — a non-super-admin's voice note is never fetched or transcribed — and notes over `WHATSAPP_VOICE_MAX_SECONDS` (default 120) are ignored. In groups a voice note only triggers the bot when it replies to one of the bot's messages; DMs always do (see docs/SECURITY.md §13 + docs/DEPLOYMENT.md §10).
- **First-contact welcome for the WhatsApp Cloud API** (#255, opt-in `WHATSAPP_CLOUD_WELCOME_ENABLED`, off by default): the Cloud API has no group-join event, so a sender's first-ever inbound message is treated as the "join" moment and gets one static welcome (community guidelines appended when set) — parity with the Discord/Baileys welcomes. Never echoes sender-supplied content, routes through the same secret-redaction path as every send, and is strictly best-effort: a DB or Graph API hiccup in the welcome path skips the welcome and never blocks the sender's real message from reaching the agent.
- `list_low_rated_knowledge` (#287): an **admin** tool that groups member ratings by knowledge entry — `list_answer_feedback` only ever showed a flat, reverse-chronological list of individual ratings, so a bad or stale FAQ entry's unhelpful ratings could pile up unnoticed. Shows entries with `unhelpfulCount >= minUnhelpful` (default 2), sorted worst-first, so an admin can jump straight to `update_knowledge`/`delete_knowledge`. Read-only, no migration, same conversation-scoped admin gate as its sibling; covers only answers served via the deterministic knowledge shortcut, same boundary `list_answer_feedback` already draws.
- `set_welcome_message` (#254): an **admin** tool to set the new-member welcome text (mirrors `set_community_guidelines` — 30s-cached, empty string clears, no CONFIRM, capped length), closing the asymmetry where guidelines were configurable but the welcome they're appended to was a hardcoded constant. The WhatsApp Cloud first-contact welcome now reads this configured text too, completing cross-platform welcome parity (#279).
- **Repeat-question shortcut** (opt-in, `REPEAT_QUESTION_SHORTCUT_ENABLED`, off by default) (#260): when the same caller sends the exact same text twice within 120 seconds — a double-tap, impatient resend, or client retry — the router replays the cached reply from their last successful turn instead of spawning a second `query()` subprocess against the shared Max pool. A third narrowly-scoped cost shortcut alongside the ack (#89) and knowledge (#163) shortcuts.

### Changed
- `list_answer_feedback` (#269) now shows the rated answer's actual text (wrapped untrusted, same convention as `list_suggestions`/`list_reports`) instead of just an opaque interaction id, and names the served knowledge entry (e.g. "served from knowledge #57") when the answer came from the deterministic knowledge shortcut — an admin acting on an unhelpful rating no longer has to dig through scrollback to see what the bot actually said. Read-only enrichment of an existing admin-gated, conversation-scoped tool; no new tool, RBAC tier, or migration.
- The two age-based retention purges (`INTERACTION_RETENTION_DAYS`, `ROSTER_DEPARTED_RETENTION_DAYS`) now get the same consecutive-failure super-admin DM alerting the context builder / knowledge refresh / docs ingest jobs already had (#291) — if either purge silently starts failing, the operator's documented deletion promise no longer goes quietly unmet with nothing but a `journalctl` line to notice. The alert names only the job and a failure count/timestamp, never the caught error or any row content. No behaviour change for a deployment that hasn't set the corresponding retention var.
- **Background-job failure alerting** (#265): the opt-in context-builder, knowledge-refresh, and docs-ingest jobs previously swallowed their own errors into a log line only — invisible to a solo operator not watching `journalctl`. Each now steps a consecutive-failure tracker that DMs super admins once when a job fails a threshold number of scheduled runs in a row (and re-arms on recovery). The alert names only the job and a failure count/timestamp, never the error or any row content. (This is the alerting the retention-purge extension in #291 later reused.)
- **te reo Māori community guidelines** (#267): `set_community_guidelines` gained an optional `language: 'en' | 'mi'` argument so an admin can store a te reo variant, and a member with a standing `set_language_preference('mi')` is served that variant — the one text most careful never to alter was also the one never adapting to the language preference. No machine translation; the admin authors the te reo text.
- `list_knowledge` gained a `staleOnly` filter (#281) so the digest's "run `list_knowledge` to review" stale-entry pointer (#199/#200) lands on exactly that subset (most-overdue first), and a `provenance` filter plus an inline `[auto]`/`[docs]`/`[admin]`/`[super_admin]` tag on every line (#296) so an admin can tell trusted docs/human entries from unreviewed `'auto'` web-research entries at a glance without opening each one.

### Fixed
- **Admin tools no longer wrongly refuse a real Discord channel they haven't seen chatter in** (#273): `announce`/`create_poll`/`create_thread` gated on a "seen this conversation before" check meant to stop privileged tools targeting arbitrary WhatsApp numbers — but on Discord the bot can only reach channels in its one configured guild, so the gate wrongly blocked e.g. a poll into a brand-new meetup channel. A new Discord `canPostTo` capability check now allows any real, sendable, in-guild channel. The refusal wording for a genuinely unreachable target was also reworded to read as a deliberate safety boundary rather than a backend bug (#275).
- **Muted-role channel overwrites now retry and alert on failure** (#277): applying the "Muted" role's deny-`SendMessages` overwrite to a channel previously bare-logged a warning on failure with no retry and no signal — so a mute could silently fail to take effect in a channel. It now retries up to three times and, if a channel still can't be locked down, sends super admins one aggregated DM naming the affected channels.
- **Retried WhatsApp Cloud webhooks are de-duplicated** (#250): Meta redelivers a webhook if the immediate ack races a slow turn, which previously produced a second full agent turn, a second reply, and an extra debit against the member's daily reply budget through no fault of theirs. The adapter now tracks recently-seen message ids and drops a redelivery.
- **Super-admin alerts now reach super admins on every platform** (#292): `notifySuperAdmins` — the path behind every audited privileged-action DM and report alert — only DMed super admins on the triggering turn's own platform, so a super admin configured only via the other platform's env list silently never got the alert. It now loops every connected adapter, matching the other alert paths. Closes a `docs/SECURITY.md` residual risk (#288).

## 2026-07-07

### Added
- Community guidelines (#212): admins can set an authoritative, curated guidelines text with `set_community_guidelines`, and members can now read the rules — ask the bot, or it points you to them when relevant. Previously the bot referenced "rule violations" as a reportable category without anything actually telling members what the rules were. Reuses the existing policies store (no migration), 30s-cached.
- `create_poll` (#228): an admin-tier tool that posts a **native Discord poll** (2–10 options, a real tallied vote with a set duration, default 24h) to a channel — for gauging interest in meetup dates, topics, and the like, instead of posting text and eyeballing reactions. Discord-only (WhatsApp cleanly reports it's unsupported), rate-capped per channel, and the question/options pass through the same outbound secret-redaction filter as every other send.
- `react_to_message` (#231): a member-tier tool so the bot can acknowledge a message with an emoji instead of always replying in text. Restricted to a closed positive/neutral allowlist — `✅ 👍 👀 🎉` — and can only react to a message it has actually seen in your own conversation.
- Assignable cosmetic Discord roles (#232): admin tools `assign_community_role` / `remove_community_role` (CONFIRM-gated, audited) and `list_assignable_roles` for handing out **purely cosmetic** roles — "verified builder", regional or interest tags. Strictly decorative and kept orthogonal to the bot's own permission tiers (super_admin/admin/member/guest still come from config + membership only, never from a Discord role).
- Source citations + freshness on knowledge answers (#214): knowledge replies now show where the answer came from (source title + link) and how recently it was verified, plus a "may be outdated" nudge for stale entries (`KNOWLEDGE_STALE_DAYS`). Rendered deterministically on the send path — never model-invented text — and citations show only for trusted (admin-curated / docs-ingested) entries, so a quarantined auto-researched entry can't gain apparent authority from a source line. Admins can attach a source when saving/updating knowledge; docs-ingest fills sources in automatically.
- Anthropic status check (#206, opt-in `STATUS_CHECK_ENABLED`, off by default): a member-tier `check_status` tool that reports Anthropic's current service status, read from Anthropic's official status page on a background poll (cached — a member's question never triggers a live fetch). Gives an authoritative "is this me or an Anthropic incident?" answer without opening web search to every member.
- `create_event` (#242): an **admin**, CONFIRM-gated, Discord-only tool that creates a real Discord **Scheduled Event** (shown in the server's Events tab with RSVP + reminders) for meetups, instead of a text announcement that scrolls away. `startTime`/`endTime` must be a concrete ISO 8601 instant with an explicit offset (relative text like "next Tuesday 7pm" is rejected at the schema boundary), start must be in the future, and the CONFIRM prompt quotes the resolved event name and start time verbatim so the admin approves the actual artifact.
- `create_thread` / `archive_thread` (#243): an **admin**, Discord-only tool pair to split a longer discussion out of the main channel flow. `create_thread` is additive/reversible so it's rate-capped (`THREAD_CREATE_RATE_LIMIT_PER_HOUR`=5/hour per channel, same shape as `create_poll`); `archive_thread` is CONFIRM-gated like `moderate`/`delete_message` since it hides an active discussion. Both are target-validated to a channel/thread the caller is scoped to and the bot has seen, and audited.
- `set_language_preference` (#192): a member/guest-tier tool to opt into always receiving replies in NZ English (`en`) or te reo Māori (`mi`) regardless of the language you write in — a standing counterpart to the existing per-message language mirroring (#69), which is unchanged for anyone who never sets a preference. Closed enum, no free text, no CONFIRM (instantly reversible), deleted by `forget_me`/`purge_user_data`.
- Admins can now **age out stale auto-moderation strikes** (opt-in, `MODERATION_STRIKE_WINDOW_DAYS`, unset by default) (#196): with a window set, only strikes newer than the window count toward `MODERATION_STRIKE_LIMIT`, so a long-ago isolated warning no longer counts the same as a recent one toward a mute. Unset preserves today's unbounded behaviour exactly.
- New admins now get an **orientation DM on `grant_admin`** (#202): a freshly-promoted admin previously picked up the admin toolset with zero signal. A fixed (non-interpolated) DM confirms the promotion and points to `community_info`'s admin rundown, firing only on an actual member→admin transition — the same "tell the person something changed" pattern as `add_member` (#76) and suggestion resolution (#116).

### Changed
- Security/privacy hardening across the untrusted-input surface (#233): the prompt-injection quarantine now sanitises display names (a crafted nickname can't inject a pseudo-directive or close the recall wrapper early); `forget_me` / `purge_user_data` erasure is completed (clears session continuity so purged messages can't survive in a resumed transcript, runs in one transaction, invalidates digests built over deleted rows); accused-admin report exclusion now follows linked identities; WhatsApp "delete/edit for everyone" is honoured only from the original author or a group admin; the CONFIRM gate deterministically surfaces the real pending action so an injected turn can't hide it behind an innocuous "reply CONFIRM"; `update_knowledge` is now CONFIRM-gated; and Discord mute enforcement covers voice/stage/media channels and threads and is reconciled on startup. No capability was widened.
- A deterministic adversarial-corpus injection gate + red-team runbook (#227) now guards prompt-injection defences against regressions in CI.
- The weekly **admin digest** gained two more signals (#195, #200): a guild-wide **pending suggestion** count (nudges `list_suggestions`) and, opt-in via `KNOWLEDGE_STALE_DAYS` (`0`/unset = off, else `>= 30`), a **stale-knowledge** count of entries neither edited nor retrieved in that many days (nudges `list_knowledge`). Both render only when non-zero; off unless `ADMIN_DIGEST_ENABLED`.
- **DM-originated content reports now reach every admin, not just super admins** (#204): a report filed from a 1:1 DM (all WhatsApp reports, and Discord DMs) had no conversation any scoped admin was ever attached to, so only the unrestricted super-admin view could see it — meaning scoped admins could never see a WhatsApp report at all. Reports are now tagged as DM-originated and made visible/resolvable by any admin. Fully closes the residual risk #90 only narrowed.

### Fixed
- Graceful shutdown (#210): in-flight turns are now drained on shutdown/restart instead of being dropped mid-reply, so a redeploy no longer cuts off an answer someone was waiting on.
- A batch of correctness fixes from a full-repo deep review (#215–#225).
- **A failing daily-reply-budget check now alerts super admins** (#205): the per-user daily-reply guardrail's error path was a bare `catch` returning 0, silently degrading the main per-user cost/abuse limit to inert for as long as the DB failure persisted, with no log or alert. It now logs at error level and fires a process-wide debounced super-admin DM. Closes a named `docs/SECURITY.md` residual risk.

## 2026-07-06

### Added
- `catch_up` (#183): a member-tier "what did I miss?" tool that recaps recent activity in your own current conversation — the on-demand, topic-agnostic recap the other tools couldn't give you (they need a search query, are FAQ-only, or are admin cross-conversation aggregates). Takes an optional `hours` window (default 24h, hard-capped server-side at 7 days), reads a plain time-range slice of the conversation, and is scoped to the caller's own conversation only.
- `my_submissions` (#161): a member-tier tool to read back your own `report_content` and `suggest_improvement` filings and their status — a pull-based fallback for when the best-effort resolution DM was missed (e.g. DMs closed). Self-scoped in SQL to your own id; it can never surface anyone else's submissions.
- `my_warnings` (#184): a member-tier, read-only tool to check your own auto-moderation status on demand — no active warnings, N active (under the limit), or limit reached — instead of having to ask an admin. Takes no arguments, so there's no id a caller could supply to read someone else's warnings; never shows the matched word or message excerpt (that stays admin-only).
- `my_data` (#190): a member-tier, read-only self-service summary of what the bot has stored about you — counts of your stored messages, knowledge entries sourced from you, reports and suggestions you filed, and your current reply-style preference — the NZ Privacy Act "access" counterpart to the existing `forget_me`/`purge_user_data` deletion path. Scope resolution matches `forget_me` exactly; points you to `my_warnings`/`my_submissions` for those specifics.
- **Knowledge shortcut** (opt-in, `KNOWLEDGE_SHORTCUT_ENABLED`, off by default) (#163): when an incoming message scores at/above `KNOWLEDGE_SHORTCUT_THRESHOLD` (default 0.9 cosine) against a curated knowledge entry, the router replies with that entry directly and skips the full agent turn (no `query()` subprocess, no Max-pool spend), with an attribution suffix so a member can always ask again for a real answer. Outbound secret-redaction/code-policy filtering still applies. A sibling flag `GUEST_KNOWLEDGE_SHORTCUT_ENABLED` (#166, also off by default) extends this to a **gated guest's** first message, but restricted to `scope='global'` FAQ entries only — so a guest can get an instant public-FAQ answer before the "ask an admin to add you" pointer, without ever reaching platform- or conversation-scoped knowledge.
- The bot now **knows the current NZ date** (#187): every system prompt is grounded with the current date and weekday (`Pacific/Auckland`, day-granularity, NZST/NZDT handled automatically), so date-relative questions ("days until the meetup", "what day is Waitangi Day this year") are answered from the real date instead of the model's training-cutoff guess.

### Changed
- Resolution DMs for suggestions and reports are now **cross-platform** (#158): `resolve_suggestion`/`resolve_report` previously only DMed the submitter when the triaging admin happened to be working from the same platform the item was filed on — so a WhatsApp filer got silently un-notified if the admin was on Discord that day. The router's per-platform adapter registry is now threaded into the tool layer so the notice always routes through the *item's origin platform*. (Supersedes the "same-platform only" caveat from #116/#120.)
- `generate_image` now posts a **caption / screen-reader alt-text** on the generated image (#186), reusing the already-validated generation prompt at no extra model-call cost. The caption passes through the same outbound secret-redaction filter as the visible message on both Discord and WhatsApp.

### Fixed
- **WhatsApp Cloud health reporting is now honest** (#185): `isConnected()` was true for the whole process lifetime once `start()` succeeded, so an expired/revoked `WHATSAPP_CLOUD_ACCESS_TOKEN` or a broken egress path to Meta never fired the sustained-disconnect super-admin alert and `/healthz` kept reporting `"whatsapp": true` indefinitely. It now tracks consecutive real send failures and flips to disconnected after three, so the existing disconnect alerting and `/healthz` actually fire on a dead WhatsApp Cloud connection.
- **Two documented mute-enforcement gaps closed** (#173): a channel or category created after a member was muted never inherited the deny-`SendMessages` overwrite, and a muted member could leave and rejoin the server to shed the role. New channels/categories now get the same overwrite applied on creation, and a rejoining member still at/over `MODERATION_STRIKE_LIMIT` is re-muted before any welcome logic runs — the latter also alerts admins, since a rejoin-triggered re-mute means an evasion attempt just happened. Both gated on `DISCORD_MODERATION_ENABLED`, matching the rest of moderation's off-by-default posture.

## 2026-07-05

### Changed
- Docs ingest now **scopes out low-value sections** (`DOCS_INGEST_EXCLUDE_PATHS`): the default drops the auto-generated per-language SDK/CLI reference (`api/go`, `api/python`, `api/typescript`, …), which was ~90% of the corpus by volume (~24k of ~29k chunks) and near-useless for a chat bot, while keeping the conceptual guides + core API. Excluded pages are neither fetched nor counted as in-index, so their previously-ingested chunks are pruned on the next run. Result: a focused few-thousand-chunk KB of the docs people actually ask about, instead of drowning them in SDK method reference. Tune via the env var (empty = ingest everything).
- Docs ingest chunks **coarser** (splits at H2 only, folding `###` subheadings inline) so API-reference pages no longer explode into thousands of one-parameter fragments — the full corpus now fits well under the (raised) chunk cap instead of truncating to the first ~16%. Pruning is now **index-based**: a `docs` chunk is removed only when its page drops out of the upstream index, not when a fetch fails — so the ~157 habitually-404 URLs in the index no longer disable cleanup. (Changing the chunking means a one-time `docs` wipe + re-ingest on the deploy that enables this.)

### Added
- Docs ingest (opt-in, `DOCS_INGEST_ENABLED`, off by default): backfills Anthropic's official developer docs into the knowledge base as RAG chunks so Dave answers API/Claude Code questions from the current documentation instead of just its training cutoff. Reads one fixed official source over HTTPS (the `llms.txt` index → each page's `.md`), chunks by heading, embeds, and diff-upserts under a `'docs'` provenance; refreshed **~weekly with a content diff** so only genuinely changed sections re-embed — and the created/updated/removed counts are the "what changed" view. Entries are **trusted** (served verbatim, shortcut-eligible), unlike the quarantined open-web `'auto'` refresh, because the source is Anthropic's own first-party docs with no model in the loop. Bounded by page/chunk caps + polite fetch concurrency; never overwrites or prunes a human-authored entry sharing a title; redeploy-safe freshness guard. See docs/SECURITY.md.
- Daily knowledge refresh (opt-in, `KNOWLEDGE_REFRESH_ENABLED`, off by default): a scheduled job web-researches a small **fixed** set of fast-moving Claude/Anthropic topics (Claude Code updates, Anthropic API/model changes) and writes each briefing straight into the knowledge base as one upserted, clearly-labelled *auto-researched* entry per topic. Deliberately the one knowledge path with **no human review gate** — so its blast radius is bounded by design: the topics are hard-coded (chat/injection can't steer them), each upserts a single entry (the base is refreshed, never grown unbounded), every entry is stamped machine-generated/unverified, search results are treated as untrusted, the run defers to a busy bot and is turn-capped, and a ~daily freshness guard makes it redeploy-safe. See docs/SECURITY.md.
- Members can now retract their own content report with `withdraw_report` — if you filed one by mistake or as a joke, you can withdraw it yourself instead of having to ask an admin (which was especially awkward when the report was *about* an admin). It only ever touches reports **you** filed (scoped in SQL to your reporter id — it can never affect anyone else's), marks them `withdrawn` and **keeps them on record** rather than deleting (so a withdrawn serious complaint stays accountable, not erased), and notifies super admins of the withdrawal so a retraction is never silent.
- Image generation (opt-in, `IMAGE_GEN_ENABLED`, off by default): an admin/super-admin `generate_image` tool creates an image from a text prompt and posts it into the conversation, on both Discord and WhatsApp. It shells out to the host's **Grok Build CLI** signed in with a SuperGrok subscription (device-code login, no API key, no per-call billing). The CLI is **locked to a single built-in tool** (`--tools GenerateImage`, no Bash/file/exec), so its unattended `--always-approve` mode has nothing dangerous to approve — it can only produce an image; the subprocess gets a **minimal scoped env** (no bot secrets — never `CLAUDE_CODE_OAUTH_TOKEN`/`DISCORD_BOT_TOKEN`/`DATABASE_URL`); the prompt is an argv element (no shell injection); never members; one-in-flight-per-user plus a per-user **daily cap** (`IMAGE_GEN_DAILY_LIMIT`); a hard timeout; and the real image format is sniffed from magic bytes rather than trusted from a filename. See docs/SECURITY.md §8.
- Knowledge-candidate review queue (opt-in, `CONTEXT_CANDIDATES_ENABLED`, off by default and a no-op while the builder itself is off): the offline context builder can now draft a Q&A candidate from a recurring, answerable question cluster — the same summarisation call that writes the digest, never a second model call. Candidates land in a `pending` queue admins browse with `list_knowledge_candidates` and turn into durable knowledge with `accept_knowledge_candidate` (which publishes via the existing `save_knowledge` path) or reject with `decline_knowledge_candidate` (non-destructive, no CONFIRM). Nothing reaches `knowledge`/`knowledge_search` without that explicit accept — the human-curation invariant this repo keeps for knowledge generally is unchanged. Closes the deferred half of #51 (#102).
- Members can now rate the bot's last answer with `rate_answer` (helpful/unhelpful, plus an optional short free-text reason, rate-capped) so admins finally get a calibrated signal on answer quality — the deferred half of #60. Admins read the aggregate, scoped to their own conversations, with the new `list_answer_feedback` tool (#118).

### Changed
- Discord replies and DMs now send with `SuppressEmbeds`, so links the bot posts no longer expand into large preview cards — the message text is unchanged, just no auto-embed.
- Auto-moderation warnings are now **public and minimal**: the warning is posted in the channel the offending message was posted in (not only the private admin channel), and names **only the member** — no user id, matched word, or message excerpt. The detailed record (id + matched term, needed for `clear_warnings`) still goes to the private admin channel, and the member still gets a DM. Follow-up to the auto-moderation feature (#141).

### Fixed
- A role change now takes effect on the target's **very next message**. Previously `grant_admin`/`revoke_admin` updated the role in the DB, but the target's live conversation session still carried the old-role framing in its history, so the bot would keep treating a freshly-promoted admin as a member (refusing admin actions) — or, worse, keep treating a freshly-*revoked* admin as an admin — until the session rolled over (30 turns / 24h). Both tools now reset the target's active-conversation session(s) on success (`clearUserSessions`, non-destructive — only session continuity is cleared, stored memory is untouched), so the new tier applies immediately.
- Membership tool replies (`add_member`, `remove_member`, `grant_admin`, `revoke_admin`, `unlink_member`) now name the member (from the membership row or the server roster) instead of echoing a raw platform id — e.g. "Granted admin to **Adam H** on discord" rather than "…to 310697646731952132". Falls back to the id only when no name is known.

## 2026-07-04

### Added
- Global crash handlers: an unhandled promise rejection is now logged at ERROR level (visible in `journalctl -u community-agent`) instead of slipping by silently, and an uncaught exception is logged and exits non-zero so systemd restarts the bot cleanly rather than leaving it in an undefined state. No more silent crashes to diagnose after the fact.
- Discord auto-moderation (opt-in, `DISCORD_MODERATION_ENABLED`, off by default): the bot scans every message for bad language / abuse, warns the member, and after `MODERATION_STRIKE_LIMIT` active warnings (default 3) assigns a "Muted" Discord role that blocks them from posting until an admin clears their warnings. Warnings and blocks post to an auto-created private `mod-alerts` channel; admins clear anyone's warnings (and lift the mute) with the new admin-tier `clear_warnings` tool. Two-stage detection keeps it cheap — a free wordlist runs on every message and an optional LLM abuse check (`MODERATION_LLM_ABUSE_ENABLED`) only escalates wordlist-clean messages. Admins and super admins are never warned or muted. Enabling it is a privacy-posture change and needs the bot to hold Manage Roles + Manage Channels — see SECURITY.md.
- Filing a `report_content` submission now proactively DMs every super admin the moment it's created, instead of relying on someone remembering to poll `list_reports` — reuses the same alert mechanism every other privileged action already triggers. No new tool, RBAC surface, or stored data; narrows (does not eliminate) the documented residual risk that DM-originated reports were only reachable by super admins (#90).
- Resolving a member's `report_content` submission now sends them a best-effort DM naming the outcome (resolved/dismissed) — closes the same "shout into the void" gap #116 fixed for suggestions, applied to safety reports. Delivered cross-platform since #158 — the notice routes through the platform the report was filed on regardless of where the admin resolves it (#120).
- Resolving a member's `suggest_improvement` submission now sends them a best-effort DM naming the outcome (reviewed/declined/done) — closes the "suggestion box into the void" gap. Delivered cross-platform since #158 — the notice routes through the platform the suggestion was filed on regardless of where the admin resolves it (#116).
- Chat-triggered redeploy: a super admin can now say "deploy the latest code" instead of waiting for the nightly timer or reaching for SSH. `redeploy_bot` takes no arguments, is CONFIRM-gated and router-executed like `grant_admin`/`purge_user_data`, and starts the same flock-guarded `community-agent-redeploy.service` unit the 1am timer uses — requires an opt-in, exact-match sudoers grant documented in DEPLOYMENT.md (#101).
- Weekly proactive admin digest (opt-in, `ADMIN_DIGEST_ENABLED`): DMs each admin at most once a week with their own scoped recurring-question clusters — the same signal `question_digest` already computes on demand, now pushed instead of pull-only. Restart-safe freshness guard, no DM on a quiet week (#97).
- Anonymised community-context export (opt-in, `CONTEXT_EXPORT_ENABLED`): context digests render into `docs/COMMUNITY-CONTEXT.md` (aggregate-only, k-floored, PII-scrubbed) so the research loop can ground proposals in real community need; committing the file stays a human step (#53).
- Offline context builder (opt-in, `CONTEXT_BUILDER_ENABLED`): a ~daily job distills stored interactions into durable topic digests admins read with `list_context_digests` — hard-capped model spend, a distinct-author floor, and purge-coherent by construction (#51).
- Ambient message archiving (opt-in, `DISCORD_ARCHIVE_ALL_MESSAGES`): every message in allowed guild channels is stored for community memory, Discord deletes/edits are honoured against the stored copy, and the bot still only replies when addressed. Requires posting the community notice from SECURITY.md first (#48).
- WhatsApp group ambient archiving parity (opt-in, per-group `WHATSAPP_ARCHIVE_GROUP_JIDS` allowlist): extends the above to WhatsApp groups — the community's largest venue — with the same posture, delete-honouring (best-effort edit-tracking), and notice precondition. Receive-side only, no new send behaviour (#103).
- In-chat suggestion capture: members can file bot-improvement ideas with `suggest_improvement` (rate-capped); admins triage the queue with `list_suggestions`/`resolve_suggestion`. The bridge to GitHub stays human (#46).
- Admin-curated member context notes: `add_member_note` / `list_member_notes` / `delete_member_note` give person-scoped facts a home outside the global FAQ — admin-only, audited, deleted by `forget_me` (#45).
- Discord server roster: join/leave events and a startup backfill persist identity metadata (never content) so admins can ask who joined, who left, and who joined but was never added as a member, via the new `list_roster` tool (#47).
- Automated nightly redeploy: `scripts/redeploy.sh` + a systemd timer fast-forward the server to `origin/main` at 1am NZ time, with build/migrate-before-restart, health-checked rollback, and a no-op fast path (#50).

- `link_member`/`unlink_member` admin tools to link a member's Discord and WhatsApp identities as one person, so `forget_me`/`purge_user_data` and the daily reply budget follow the person instead of the platform row (#44).
- `community_info` now names concrete member capabilities — `report_content`, `forget_me`, `suggest_improvement`, `remember_search`, `knowledge_search` — instead of a vague static blurb, and the one-time approval DM signposts this self-serve rundown (#92).
- **Typing indicator while the bot is thinking** (#79): every platform now shows a typing/presence indicator during the multi-second gap between addressing the bot and getting a reply (re-fired every ~8s while the turn is in flight), so "we're working on it" no longer reads as "the bot is broken". Best-effort and fire-and-forget by contract — a slow or failing indicator can never delay or block the reply.
- **WhatsApp group-join welcome** (opt-in, `WHATSAPP_WELCOME_ENABLED`, off by default, Baileys) (#86): a new participant joining an allowed WhatsApp group triggers one static, non-agent welcome posted to the group itself (never a 1:1 DM to the joiner, the higher ban-risk pattern; never names or @-mentions them), with a cooldown (`WHATSAPP_WELCOME_COOLDOWN_MINUTES`, default 180) — onboarding parity with the Discord welcome.
- **Acknowledgement shortcut** (opt-in, `ACK_SHORTCUT_ENABLED`, off by default) (#89): a pure acknowledgement to the bot ("thanks", "ok", "👍" — an exact-match fixed list) skips the expensive agent turn (memory recall + a `query()` subprocess) and sends one static "No worries!" reply. Exact-match only; anything carrying real content falls through to the agent.
- `set_response_style` (#129): a member/guest-tier tool to opt into simplified, jargon-free replies once instead of re-asking every message — a standing accessibility preference (`standard`/`plain`, closed enum, no CONFIRM, reversible). Deleted by `forget_me`/`purge_user_data`; surfaced in `community_info`.
- Recalled and searched messages now carry a **Discord jump link** (#138): a Discord-origin memory hit (automatic per-turn recall or `remember_search`) with a stored message id gets a clickable link back to the original message, so members can see the full thread/reactions/attachments. WhatsApp hits and older pre-archiving rows are unaffected.
- **Departed-member roster retention purge** (opt-in, `ROSTER_DEPARTED_RETENTION_DAYS`, unset/0 = disabled) (#139): `server_roster` previously kept identity metadata for everyone who ever joined, indefinitely — flagged twice in `docs/SECURITY.md` as a deferred refinement. With a value set (floor 30 days, comfortably clear of `list_roster`'s 7-day churn window), rows for members who left longer ago than that are age-purged; currently-present members are never touched. Mirrors the `INTERACTION_RETENTION_DAYS` pattern (#12).

### Changed
- `moderation_history` can now **filter by target member and action kind** (#83), so an admin checking one member's prior history before escalating no longer has to eyeball a mixed feed that could scroll past the 100-row cap. Mirrors the `status` filter `list_reports` already had.
- `save_knowledge` now **warns admins of a near-duplicate on save** (#94): before inserting, it runs a same-scope top-1 similarity check and, if the new entry clears a high threshold (0.92) against an existing one, appends a one-line nudge naming the existing entry and suggesting `update_knowledge` instead. The save always happens — it's a nudge, not a block.
- `knowledge_search` now applies a **relevance floor** (#98): hits below `KNOWLEDGE_SEARCH_RELEVANCE_THRESHOLD` (0.35) are dropped rather than returned as weak, off-topic matches, so the "say so plainly when nothing relevant is found" rule is actually enforceable and retrieval-count tracking only counts genuinely-relevant hits.
- The weekly **admin digest** now folds in **pending access-request and open-report counts** (#135) — a waiting guest or an untriaged report no longer sits silently in `list_access_requests`/`list_reports` until an admin thinks to poll it. Dedicated `COUNT(*)` queries, so a backlog larger than those tools' page limits is never understated.
- **Knowledge retrieval is now tracked** (#140): each `knowledge` entry records a `retrieval_count`/`last_retrieved_at` (bumped fire-and-forget on a relevant `knowledge_search` hit), surfaced in `list_knowledge`, so admins can spot and prune entries that are never actually retrieved.
- **Repeated identical-text abuse checks are now deduped** (#264): the opt-in LLM abuse classifier (`MODERATION_LLM_ABUSE_ENABLED`) fires on every in-scope message the free wordlist doesn't already catch, so a copy-pasted spam/phishing burst previously paid for the same classification N times over. Identical text in the same guild/channel within a 5-minute window now reuses the first verdict instead of re-calling the model; a different channel or platform is always a fresh call, and a call that errors is never cached, so one transient failure can't suppress reclassification of an entire burst.

### Fixed
- A database hiccup during memory recall or session lookup no longer makes the bot go silent — the turn degrades (answers without memory context / starts a fresh session) and a router backstop guarantees the member always gets a reply (#52).
- `knowledge_search` now enforces the `scope` an entry was saved with: an admin's channel- or platform-scoped FAQ no longer surfaces to every member, everywhere — previously `scope` was write-only metadata, decorative at read time (#106).
- Review polish: the WhatsApp Cloud app secret is redacted from logs, dead `recentTurns` code removed, and the `usage_stats` window clamped to a sane range (#110).
- **The bot now tells a member it's paused instead of going silent** (#130): while `pause_bot` is active, a member's addressed message previously hit a bare return — indistinguishable from the bot being broken. It now sends a debounced pause notice (super admins still bypass the pause). Same "silence reads as broken" pattern as the rate-limit notice.
- **A Claude usage-limit/overload failure is now distinguished from a generic error** (#132): `execTurn` returned the same "internal error, please try again" for every exception, including a genuinely-exhausted shared Max pool. A matched upstream usage-limit condition now returns a distinct, honest member reply and DMs admins (fixed strings only — the raw error is never echoed).

## 2026-07-03

### Added
- `report_content` member tool plus `list_reports`/`resolve_report` admin tools for flagging harassment/spam/rule violations to a conversation's admins (#70).
- `moderation_history` admin tool, scoped to the admin's own conversations (#34).
- `question_digest` admin tool to surface recurring questions (#23).
- Proactive super-admin alert when the shared Max-pool usage budget runs high (#25).
- `/healthz` endpoint and sustained-disconnect super-admin alerting (#14).
- Age-based retention purge for raw interactions, for privacy (#12).
- Persona registry with a default voice (#19; the default was later renamed from "Kaha" to "Dave" in #57).
- Shared VISION.md rubric; richer research and adversarial prompts (#18).
- Postgres + pgvector CI service and repository integration tests (#15).
- Build + PR-review GitHub Actions using Claude Max subscription auth (#20).
- A newly-approved member is now DMed the moment `add_member` approves them (#76), instead of having to guess they've been unlocked or wait for an out-of-band ping — a short fixed confirmation, sent only on an actual non-member→member transition (re-running on an existing member sends nothing), best-effort so a closed-DM failure never breaks the approval.

### Changed
- The bot now briefly attributes answers backed by community knowledge, and
  flags community-specific answers with no knowledge-base match as general
  knowledge rather than a confirmed fact (#60).
- Outbound replies now convert Discord-style markdown to WhatsApp-readable formatting (#38).
- Long WhatsApp Cloud API replies are chunked under Meta's 4096-character limit (#36).
- `knowledge_search` surfaces how recent each knowledge entry is (#32).
- Build-worker turn cap raised to 300 with a matching 60-minute timeout (#49; earlier 40 → 80 in #31).
- PR-review worker can now review build-worker PRs (#33).
- Pipeline loops documented as cloud Routines (#16).
- The bot now **mirrors the member's language** instead of always replying in NZ English (#69): when a member writes in another language, the reply follows suit (te reo Māori replies kept simple and short with macrons preserved; Claude/API terms, product names, and code left untouched). Mixed-language messages and genuine uncertainty default back to NZ English so a "Kia ora" greeting doesn't flip a whole English reply.
- `usage_stats` now **breaks cost down by role tier** (#67): a super admin sees `cost_usd` and reply counts attributed across admin / member / super_admin, so the documented WebSearch-tier cost asymmetry (admin+ turns carry an extra tool) is finally visible rather than inferred.

### Fixed
- Membership tools (`add_member`, `grant_admin`, `remove_member`, `revoke_admin`) can now target either platform via an optional `platform` argument instead of always assuming the caller's; ids are shape-validated so a WhatsApp number can no longer be filed as a Discord user (#78).
- Blank optional numeric env vars (e.g. `HEALTH_PORT=`) no longer fail config validation (#40).
- Build-worker verify step hardened against spoofed or stale PRs (#30).
- Build worker uses an explicit allowedTools list and deterministic PR verification (#29).
- PR-review worker does read-only reviews and always posts a deterministic verdict (#26, #24).
- Members are now notified when they hit the per-user rate limit (#65): tripping the 8-messages/60s limit previously returned nothing at all — silence that reads as the bot ignoring or breaking. It now sends a debounced notice via the normal send path, matching the daily-reply-budget notice a few lines below it.

## 2026-07-02

### Added
- Three-tier RBAC (super_admin / admin / member) with gated access, plus security and correctness fixes and dependency upgrades (#1).
- WhatsApp Cloud adapter against the official Meta Cloud API (#6).
- Knowledge curation tools: list, update, delete (#8).
- Proactive onboarding: a Discord welcome message and an access-request queue (#10).
- Multi-loop pipeline scaffolding: labels, docs, conventions (#2).

### Fixed
- `setup-labels` workflow: restored `contents:read` so checkout works (#3).
