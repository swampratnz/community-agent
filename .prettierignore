dist/
whatsapp-auth/
node_modules/
package-lock.json

# Docs are hand-formatted prose/tables; keep Prettier scoped to code for now.
*.md

# This bot's push token lacks the `workflows` GitHub App scope, so it can
# never commit a change under .github/workflows/ — excluded so format:check
# doesn't fail on files this pipeline is structurally unable to fix.
.github/workflows/
